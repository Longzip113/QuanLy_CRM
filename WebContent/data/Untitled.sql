ACCOUNTSCREATE database SERVLET_CMD

CREATE TABLE ACCOUNTS(
	ACCOUNT_ID INT AUTO_INCREMENT,
    FULLNAME VARCHAR(100) NOT NULL,
    EMAIL VARCHAR(100),
    PASSWORD VARCHAR(20),
    ADDRESS VARCHAR(20),
    PHONE VARCHAR(20),
    PRIMARY KEY (ACCOUNT_ID)
)

insert into ACCOUNTS(FULLNAME,EMAIL,PASSWORD, ADDRESS, PHONE) values('admin','long123@','admin','TP.HCM',12342423);
insert into ACCOUNTS(FULLNAME,EMAIL,PASSWORD, ADDRESS, PHONE) values('nguyenvana','long123@','nguyenvana','HN',22131312);
insert into ACCOUNTS(FULLNAME,EMAIL,PASSWORD, ADDRESS, PHONE) values('nguyenvanb','long123@','nguyenvanb','BD',21231311);


USE SERVLET_CMD

CREATE TABLE GROUPSS(
	GROUP_ID INT AUTO_INCREMENT,
    GROUP_NAME VARCHAR(255) NOT NULL,
    DESCRIPTION VARCHAR(255),
    PRIMARY KEY (GROUP_ID)
)

CREATE TABLE STATUS(
	STATUS_ID INT AUTO_INCREMENT,
    STATUS_NAME VARCHAR(255) NOT NULL,
    PRIMARY KEY (STATUS_ID)
)

CREATE TABLE TASKS(
	STASK_ID INT AUTO_INCREMENT,
    STASK_NAME VARCHAR(100) NOT NULL,
    END_DATE VARCHAR(20),
    GROUP_ID INT,
    ACCOUNT_ID INT,
    STATUS_ID INT,
    PRIMARY KEY (STASK_ID),
    FOREIGN KEY (GROUP_ID) REFERENCES GROUPSS(GROUP_ID),
    FOREIGN KEY (ACCOUNT_ID) REFERENCES ACCOUNTS(ACCOUNT_ID),
    FOREIGN KEY (STATUS_ID) REFERENCES STATUS(STATUS_ID)
)


select* from ACCOUNTS 











